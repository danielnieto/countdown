{
  "name": "parcel-sandbox",
  "version": "1.0.0",
  "description": "Simple Parcel Sandbox",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /countdown",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.6.1",
    "parcel": "^1.9.7"
  }
}
